// Generated by CoffeeScript 1.11.1
(function() {
  var Chai, adb, client, expect;

  Chai = require('chai');

  expect = Chai.expect;

  adb = require('../adb');

  client = adb.createClient({
    host: 'localhost',
    port: 5078
  });

  describe('InstallRemoteCommand', function() {
    return it("should send 'adb installRemote file' <right>", function() {
      return client.installRemote('123456', '/data/local/tmp/STFserver.apk', function(err, data) {
        if (err === null) {
          return expect(data).to["true"];
        } else {
          expect(err).to.be.an["instanceof"](Error);
          return expect(err.message).to.equal('the command is not support');
        }
      }).then(function(data) {
        return expect(data).to.equal(true);
      })["catch"](function(err) {
        expect(err).to.be.an["instanceof"](Error);
        return expect(err.message).to.equal('the command is not support');
      });
    });
  });

}).call(this);

//# sourceMappingURL=InstallRemoteCommand.js.map
